import { HeaderContainer } from "./header.styles"
import Logo from "./logo/logo"

const Header = () => {
    return (
        <HeaderContainer>
            <Logo />
        </HeaderContainer>
    )
}

export default Header